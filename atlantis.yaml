version: 3
projects:
  - name: dev
    dir: environments/dev
    autoplan:
      when_modified: ["environments/dev/**", "modules/**", "atlantis.yaml"]
      enabled: true
  - name: prod
    dir: environments/prod
    autoplan:
      when_modified: ["environments/prod/**", "modules/**", "atlantis.yaml"]
      enabled: true
    


